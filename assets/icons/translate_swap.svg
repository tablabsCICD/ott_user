<svg viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- White card -->
  <rect x="40" y="40" width="200" height="280" rx="32" fill="none" stroke="currentColor" stroke-width="20"></rect>

  <!-- Red card (REMOVE hardcoded red) -->
  <rect x="180" y="80" width="240" height="280" rx="32" fill="none" stroke="currentColor" stroke-width="20"></rect>

  <!-- Letter A -->
  <text x="140" y="220" font-size="120" font-weight="700" fill="currentColor" text-anchor="middle">A</text>

  <!-- Letter अ -->
  <text x="300" y="260" font-size="120" font-weight="700" fill="currentColor" text-anchor="middle">अ</text>

  <!-- Arrows -->
  <path d="M350 40 C420 40 420 120 350 120" fill="none" stroke="currentColor" stroke-width="18" stroke-linecap="round"></path>
</svg>
